# review-studio

这是一个纯静态的人工校对页面模板。

它不依赖后端服务，适合把单个复核批次导出为一个可直接分享的网址：

- 左侧显示原始页图和检测框
- 右侧显示当前系统判断和复核列表
- 点击框和列表会同步高亮
- 可直接标记“正确 / 错误 / 跳过”
- 结果保存在浏览器本地，并可导出成 JSON / CSV

当前推荐搭配 `dataset-tools` 的 `export-review-site` 命令使用。
